name: spectral
class: tdisplay-esp32-ch9102
display_name: Spectral
blurb: Host metrics shown as colored bars and a spectrum display
version: "1.0.0"
mount: usb-down

# A single-variant faceplate bundled at this root. To add mounts,
# add a variants list and move the bundle
# into per-mount directories (down-mount/ etc.) beside this manifest.
