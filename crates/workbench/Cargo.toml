[package]
name = "suzu-workbench"
description = "Suzu's desktop workbench: the keeper's window onto the Resident and its faces"
publish = false
version = "0.1.0"
edition = "2021"
license = "MIT"
build = "build.rs"

[dependencies]
serde_json = "1"
tauri = { version = "=2.11.5", features = ["image-png", "tray-icon"] }

[build-dependencies]
tauri-build = "=2.6.3"
