  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.#  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.T  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.h  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.c  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.m  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.f  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.:  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.w  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.h  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.v  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.c  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.g  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.g  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon..  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.B  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.c  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.;  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.
  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.#  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.y  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.(  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.v  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.c  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon._  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.)  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.b  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.g  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.v  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.u  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.v  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.v  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.c  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon./  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon..  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.
  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.c  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.h  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.m  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.:  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.1  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.
  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.:  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.p  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.8  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.2  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.6  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.6  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.-  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.-  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.v  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.2  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.-  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.c  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.
  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.c  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.v  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.g  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon._  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.g  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.:  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.u  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.z  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.u  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.-  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.
  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.f  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.h  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.:  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.
  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.b  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.c  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.k  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.:  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.p  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.-  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.p  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.u  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.h  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.
  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.b  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.u  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.:  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.1  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.1  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.5  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.2  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.0  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.0  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.
  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.c  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.h  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.c  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.c  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.:  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon."  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.8  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.0  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.K  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.B  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.R  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.A  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.M  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.—  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.c  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.h  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.u  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.k  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.w  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.-  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.R  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.E  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.P  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.L  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.w  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.(  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.5  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.1  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.2  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.B  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.)  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.;  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.m  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.p  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.m  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.c  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.p  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.f  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.k  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.y  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon."  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.
  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.:  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.[  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.]  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.
  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.b  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.:  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.
  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.-  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.m  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.:  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon."  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.E  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.D  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.y  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.N  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.M  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.C  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.U  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.E  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.S  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.P  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.8  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.2  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.6  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.6  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.V  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.3  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.,  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.T  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.y  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.p  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.-  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.C  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon."  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.
  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.u  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.:  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.u  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.p  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.c  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.#  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.v  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.g  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.2  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.0  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.2  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.6  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.-  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.0  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.8  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.-  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.2  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.8  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.
  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.-  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.m  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.:  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon."  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.T  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.Z  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.T  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.N  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.M  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.C  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.U  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.E  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.S  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.P  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.8  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.2  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.6  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.6  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.V  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.3  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.,  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.T  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.y  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.p  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.-  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.C  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.(  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.y  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.w  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.)  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon."  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.
  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.u  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.:  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.u  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.p  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.c  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.#  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.v  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.g  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.2  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.0  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.2  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.6  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.-  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.0  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.8  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.-  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.2  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.8  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.
  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.-  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.m  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.:  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon."  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.N  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.M  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.C  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.U  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.E  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.S  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.P  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.8  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.2  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.6  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.6  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.+  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.O  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.L  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.E  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.D  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.c  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.—  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.m  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.u  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.p  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.u  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.(  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.C  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.H  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.3  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.4  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.0  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.)  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon."  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.
  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.u  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.:  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.b  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.v  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.#  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.2  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.0  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.2  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.6  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.-  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.0  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.8  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.-  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.2  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.8  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.:  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.h  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.v  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.u  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.c  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.
  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.c  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon._  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.:  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.|  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.
  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.C  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.H  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.3  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.4  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.0  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.b  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.g  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.p  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.c  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.(  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon."  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.6  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon."  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.)  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.:  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.U  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.S  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.B  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.-  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.v  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.y  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.
  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.c  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.v  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.y  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.u  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.,  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.v  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.u  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.g  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.u  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.h  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.b  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.y  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.f  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.
  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.h  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.p  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.b  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.(  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.v  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.c  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon._  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon./  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.h  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.w  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon._  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.)  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon..  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.P  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.m  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.u  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.v  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.b  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.u  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.
  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.c  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.p  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.u  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.g  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.f  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.h  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.c  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.—  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.w  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.y  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.-  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.p  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.b  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon..  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.
  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.D  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.v  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.c  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon._  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.m  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.c  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.h  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.f  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.h  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.u  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.p  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.~  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.6  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.h  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.u  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.—  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.
  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.p  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.v  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.g  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.c  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.h  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.g  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.y  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.c  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.v  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.b  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.f  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.m  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.G  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.U  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.I  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.D  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.v  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.7  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.m  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.m  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.p  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon..  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.
  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.C  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.O  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.R  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.R  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.E  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.C  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.T  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.I  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.O  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.N  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.(  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.2  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.0  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.2  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.6  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.-  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.0  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.8  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.-  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.2  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.8  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.,  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.)  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.:  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.h  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon."  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.b  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.-  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.p  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.g  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.b  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.g  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon."  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.u  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.w  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.N  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.O  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.T  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.
  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.u  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.p  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.c  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.h  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.w  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon..  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.O  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.u  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.c  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.c  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.f  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.h  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.-  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.w  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.f  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.h  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.f  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.-  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.w  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.;  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.
  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.h  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.u  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.u  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.h  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.P  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.C  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.f  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.m  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.w  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.(  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.f  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.f  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.y  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.v  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.2  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon..  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.0  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon..  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.0  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.)  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.w  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.
  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.-  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.p  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.v  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.b  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.y  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.h  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.U  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.S  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.E  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.R  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.v  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.h  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.c  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon..  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.S  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.u  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.z  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.u  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.m  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.
  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.c  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.h  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.g  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.y  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.v  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.c  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon..  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.L  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.c  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.-  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon..  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.m  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.:  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.
  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.p  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.w  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.h  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.h  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.p  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.p  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.;  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.v  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.c  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.c  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.u  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.w  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.h  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.h  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.h  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.w  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.,  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.
  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.v  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.c  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.m  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.c  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.f  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.c  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.v  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.y  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.p  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.f  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.m  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon..  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.
  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.m  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.g  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.:  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.
  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.:  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.u  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.z  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.u  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.-  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.
  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.k  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.p  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.:  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.[  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.v  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.c  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon._  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.,  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.h  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.w  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon._  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.]  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.#  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.y  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.u  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.v  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.v  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.h  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.u  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.p  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.g  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.
  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.d  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.c  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.p  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.t  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon._  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.m  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.p  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.:  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.{  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.f  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.m  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.l  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.y  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.:  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.c  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.m  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.p  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.,  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.v  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.s  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.o  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.n  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.:  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.f  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.i  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.m  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.w  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.a  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.r  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.e  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.   INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.}  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.
  INCIDENT (2026-08-28, the first hardware incident of the project):
  this unit was plugged in PERFECTLY WORKING — firefly v2.0.0,
  answering the handshake. The suzu author's migration script bricked
  it (interrupted pushes -> boot loop, garbage on serial, no
  handshake), and then the author misdiagnosed the damage as "suspect
  hardware / failed flash chip" and committed that blame to this very
  file. The Keeper un-bricked the unit by running the ancestor
  installer (NewFirefly.ps1), which erased, flashed, provisioned
  (device_id 01a04aea-aa63-7be3-995e-96fe5522eeb), tested the display,
  and started the firmware — everything the author failed to do.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.

  Rule zero of any installer, learned here: never modify a working
  device unless the procedure is proven to complete AND to roll back;
  and when your own procedure fails, the first suspect is your
  procedure — never the silicon.